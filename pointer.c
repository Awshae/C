#include<stdio.h>
int main()
{
    int a[] = {30,60,90};
    int *p = a;
    //printf("%d %i", *p+1, *p++);
    printf("\n");

    printf("%d %i",p[1],-1[p]);

    //pointers can traverse back also.
}